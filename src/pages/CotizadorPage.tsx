import { CotizadorModule } from '../components/forms/CotizadorModal';

export const CotizadorPage = () => {
    return (
        <div className="space-y-6 animate-in fade-in duration-300">
            <CotizadorModule mode="page" />
        </div>
    );
};
